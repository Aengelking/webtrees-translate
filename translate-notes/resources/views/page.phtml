<?php

declare(strict_types=1);

/**
 * A custom page (a built-in replacement for the "Simple Menu" module).
 *
 * The body is authored HTML and is rendered as-is (an administrator wrote it).
 * The wt-tn-page-title / wt-tn-page-body classes are what this module's own
 * front-end script looks for, so the title and body are translated into the
 * visitor's language automatically — the same machinery as note translation.
 *
 * @var \Fisharebest\Webtrees\Tree|null $tree
 * @var string                          $title
 * @var string                          $body
 */

?>

<h1 class="wt-tn-page-title"><?= e($title) ?></h1>

<div class="wt-tn-page-body">
    <?= $body ?>
</div>
